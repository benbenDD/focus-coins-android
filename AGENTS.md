# 项目协作约定

## 版本与提交

- 每个可交付版本使用一个独立 Git 提交，不把多个版本混在同一提交中。
- 版本号、源码、APK 和更新说明必须对应同一次提交。
- 如果当前目录尚未初始化 Git，不伪造提交记录；先明确告知，再由项目负责人决定是否初始化仓库。

## 检查与构建

- 只运行能够发现本次改动具体失败、且失败后会改变下一步处理方式的测试。
- 完成对应测试后，只进行一次最终 APK 构建。
- 不为“更放心”重复构建、重复检查或生成内容相同的报告。

## 默认交付

每个版本默认交付：

- 可安装 APK；
- 对应源码和 Git 提交；
- 简短更新说明；
- 实际运行的测试及结果。

交付说明只报告真实完成的内容。没有执行的测试、没有生成的提交或没有完成的真机验证必须明确说明。

## 发布

- 日常开发版本只生成本地 APK，不自动创建 GitHub Release。
- 仅在明确进入发布流程时创建 GitHub Release，并使用已经验收的版本提交和 APK。

## 输出范围

- 不生成后续流程无人读取的哈希、校验和、指纹文件。
- 不生成重复的测试报告、构建报告或版本总结。
- CURRENT.md 是当前状态的唯一简短入口；完成新里程碑后直接更新它，不追加历史流水账。
