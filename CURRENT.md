# 当前里程碑

专注金币 v0.4.2：保护服务运行期间，每 3 秒刷新金币通知；在 Android 13/14 允许用户滑走前台服务通知的限制下，被滑掉的通知会自动恢复。

## 已完成

- 不受限应用直接打开，不提问、不投入金币。
- 固定受限应用按预计分钟数预付金币；小红书包含在固定受限名单中。
- 固定名单不可在软件中更改，其他应用可自行设为免费或受限。
- 预计时间到后提供锁屏、回到主屏幕、重新登记继续使用三种选择。
- 提前离开应用不退还金币。
- 顶部常驻通知显示当前金币。
- 前台服务被划掉后安排恢复，并提供关闭电池优化入口。
- 前台应用识别窗口由数秒扩大到一分钟，并增加最近使用数据回退，减少解锁后漏判。
- 新增系统绑定的无障碍应用监听，作为应用切换的主要触发源。
- 解锁成功后固定显示目的和预计分钟登记。
- 预计时间改为手动输入 1–1440 分钟。
- 登记遮罩延伸到刘海区域。
- 屏幕顶部显示小型金币图标和余额；系统通知继续作为备用显示。
- 每天 07:00 发放 30 启动金币，再扣除到期债务。
- 修正借金币确认框中的旧版扣费说明。
- 预计分钟输入框默认留空。
- 无障碍窗口事件延迟 450 毫秒合并，只处理稳定后的最新前台应用，减少离开设置后误弹。
- 回到桌面或专注金币首页时明确结束前台应用会话，避免旧应用状态残留。
- 首页“手机后台保护”提供后台自启动、电池无限制、最近任务加锁三步引导；无法由系统读取的项由用户确认。
- 修复自行设为受限/免费时勾选结果未保存的问题。
- 输入法窗口不再被误当成新打开的应用；小米手机管家归入不受限系统应用。
- 点击“取消并回到主屏幕”后，短时忽略返回桌面途中残留的同应用事件，避免重复弹窗。
- 取消按钮改为白底青色描边的全宽按钮，与主操作保持明显间距。
- 每个应用的已登记有效期独立持久保存；预计时间未到时，即使经过键盘、系统窗口、短暂切换或保护服务重启，返回同应用也不重复提问。
- 小米搜索、智能助理、语音助理、音质音效、息屏、壁纸与背屏系统组件加入固定不受限名单。
- 小米系统界面插件、辅助功能菜单、内容扩展/自带翻译和扫描组件加入固定不受限名单。
- 每天 07:00 根据前一天屏幕使用时长生成分档评语：从一小时内的夸奖到超过八小时的幽默提醒，并配套表情。
- 每天 07:00 将前一天剩余金币按“1 金币 = 1 朵红玫瑰”记入每日收藏；负余额不会产生负数玫瑰。
- 每日评语是第二天对前一日的总结，文案统一使用“昨天”；放假建议使用“今天”。
- 今日概览和每日收藏中的屏幕时长统一显示为“X 小时 Y 分钟”。
- 每天 07:00 结算后，当天第一次解锁会显示一次顶部胶囊提醒，包含玫瑰、屏幕时长和评语；显示约 6 秒后再弹出解锁目的提问。
- 固定规则文案已按源码名单补全系统界面、搜索、助理、翻译、扫描、背屏等不受限类别，以及固定受限应用品牌。
- 紧急借用移至独立区域：每借 15 金币，次日 07:00 偿还 30；多次累计，次日余额可为负数。
- 常驻通知设为前台服务类别、不自动取消并要求立即显示；Android 仍保留关闭通知、停止应用和强制停止的系统权限。
- 金币通知同时设置持续与不可清除标记；保护服务存活时每 3 秒重新发布一次，滑动清除后会自动恢复。
- 每日最多借币 2 次，每次 15，最多 30；次日 07:00 将全部待偿还金额一次扣清。
- 今日概览的获得和消耗改用金币图标标注。
- 笔记/备忘录、计算器、录音机、日历、时钟/闹钟、相机、文件管理和下载管理加入固定不受限名单。
- 受限名录中的“哔哩哔里”修正为“哔哩哔哩”。
- 首页计数和选择列表使用有效视图：不计入已属于固定免费/固定受限、已卸载、不再可选或同时出现在两组的旧项目；原始选择不删除。
- 金币悬浮标记移到屏幕顶部正中。
- 采用薄荷绿、白色卡片和深青主色重做首页及登记界面。
- 使用全新的金币与专注光芒启动图标。

## 待真机验收

1. 授予应用监听、使用情况、悬浮窗、通知、设备管理和后台运行权限。
2. 打开固定免费应用，确认要求登记但扣除 0 金币。
3. 打开小红书或其他固定受限应用，确认按预计时间预付金币。
4. 等到预计时间结束，分别验证三种结束选择。
5. 划掉专注金币后台任务，确认顶部金币标记、常驻通知和应用限制仍有效。
6. 连续锁屏、解锁并打开不同应用，确认每次开始使用都会出现登记页。

v0.4.2 已通过 Java 编译检查和最终 APK 构建，已覆盖安装到已连接手机；版本号 19 / 0.4.2。真机确认 FocusService 为前台服务，金币通知 id=7，通知 flags=0x6a（包含持续与不可清除标记）。测试时手机处于锁屏界面，未能代替用户完成通知横向滑动；自动恢复需用户解锁后手动验收。

项目目录当前未初始化 Git，因此本版本没有对应 Git 提交。
