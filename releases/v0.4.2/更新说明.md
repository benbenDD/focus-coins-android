# 专注金币 v0.4.2

- 金币前台服务通知补充持续通知与不可清除标记。
- 保护服务运行期间每 3 秒重新发布通知；在 Android 13/14 允许用户滑掉前台服务通知的系统规则下，通知被滑掉后会自动恢复。
- 系统仍允许用户关闭应用通知、从“正在运行的应用”停止服务或强行停止应用；普通应用无法绕过这些系统控制。

## 验证

- Java 编译检查：通过，仅有项目原有的旧接口提示。
- 最终 APK 构建：通过。
- 真机覆盖安装：通过，版本号 19 / 0.4.2，原有数据保留。
- 前台服务：运行中，通知 id=7。
- 通知标记：flags=0x6a，包含持续与不可清除标记。
- 实际横向滑动：测试时手机处于锁屏界面，未代替用户执行；需解锁后手动确认通知会在约 3 秒内恢复。
